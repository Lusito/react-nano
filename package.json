{
  "private": true,
  "workspaces": [
    "packages/redux",
    "packages/redux-dynamic-modules",
    "packages/router",
    "packages/tsrux",
    "packages/use-event-source",
    "packages/use-graphql",
    "packages/use-fetch"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "docs:build": "rimraf docs-dist && mono-docs . docs-dist",
    "docs:dev": "nodemon --ignore \"docs-dist\" --ignore node_modules --ignore dist -e ts,tsx,md,scss,png,webp --exec \"npm run docs:build -- serve\"",
    "lint": "mono-lint",
    "lint:fix": "mono-lint --fix",
    "release": "mono-release",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "@lusito/eslint-config-react": "^2.0.3",
    "@lusito/mono": "^0.8.2",
    "@lusito/mono-docs": "^0.8.4",
    "@lusito/prettier-config": "^2.0.0",
    "eslint-plugin-jest": "^26.3.0",
    "nodemon": "^2.0.20",
    "prettier": "^2.6.2",
    "sort-package-json": "^1.57.0"
  }
}
